alias ll="ls -lart"

alias s="source ~/.zshrc"
alias ealias="$EDITOR $HOME/.zsh/shell_aliases"

# Git
alias gs="git status"
alias gc="git commit"
alias gd="git diff"

function cdr() {
    dir="${HOME}/repos"
    if [[ -d $1 ]]; then
        dir="${dir}/${1}"
    fi
    cd $dir
}

